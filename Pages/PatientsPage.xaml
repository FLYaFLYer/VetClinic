<Page x:Class="VetClinic.Pages.PatientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Пациенты">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Используем UniformGrid для одинакового размера кнопок -->
        <UniformGrid Grid.Row="0" Rows="1" Columns="4" Margin="10" HorizontalAlignment="Left">
            <Button x:Name="btnAddPatient" Content="Добавить" 
                    Style="{StaticResource NavButtonStyle}"
                    Click="BtnAddPatient_Click"/>
            <Button x:Name="btnEditPatient" Content="Изменить"
                    Style="{StaticResource NavButtonStyle}"
                    Click="BtnEditPatient_Click"/>
            <Button x:Name="btnDeletePatient" Content="Удалить"
                    Style="{StaticResource NavButtonStyle}"
                    Click="BtnDeletePatient_Click"/>
            <Button x:Name="btnViewHistory" Content="История"
                    Style="{StaticResource NavButtonStyle}"
                    Click="BtnViewHistory_Click"/>
        </UniformGrid>

        <DataGrid x:Name="dataGrid" Grid.Row="1" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Кличка" Binding="{Binding Name}" Width="100"/>
                <DataGridTextColumn Header="Вид" Binding="{Binding AnimalType.Name}" Width="80"/>
                <DataGridTextColumn Header="Порода" Binding="{Binding Breed.Name}" Width="100"/>
                <DataGridTextColumn Header="Владелец" Width="150" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Owner.FullName" Mode="OneWay"/>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Возраст" Binding="{Binding AgeFormatted}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Вес" Binding="{Binding Weight, StringFormat=N2}" Width="60"/>
                <DataGridTextColumn Header="Цвет" Binding="{Binding Color}" Width="80"/>
                <DataGridTextColumn Header="Особые приметы" Binding="{Binding DistinctiveFeatures}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>