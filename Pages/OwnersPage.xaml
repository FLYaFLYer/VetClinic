<Page x:Class="VetClinic.Pages.OwnersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Владельцы">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnAddOwner" Content="Добавить" 
                    Style="{StaticResource NavButtonStyle}"
                    Click="BtnAddOwner_Click"/>
            <Button x:Name="btnEditOwner" Content="Изменить" Margin="10,0,0,0"
                    Style="{StaticResource NavButtonStyle}"
                    Click="BtnEditOwner_Click"/>
            <Button x:Name="btnDeleteOwner" Content="Удалить" Margin="10,0,0,0"
                    Style="{StaticResource NavButtonStyle}"
                    Click="BtnDeleteOwner_Click"/>
            <TextBox x:Name="txtSearch" Width="200" Margin="10,0,0,0"
                     TextChanged="TxtSearch_TextChanged"/>
        </StackPanel>

        <DataGrid x:Name="dataGrid" Grid.Row="1" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="120"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page> 